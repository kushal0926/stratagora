services:
    frontend:
        build:
            context: ./web
            target: builder # Stop at builder stage for dev
        volumes:
            - ./web:/app
            - /app/node_modules
            - /app/.next
        command: npm run dev

    backend:
        volumes:
            - ./backend:/app
        command: go run cmd/api/main.go
